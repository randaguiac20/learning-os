# Done — a real Python project, in miniature

In 25 minutes you:

- Built an **isolated `.venv`** and proved isolation with `which python` — the system Python untouched.
- **Installed a package** (pytest) into the venv and confirmed it importable there.
- Wrote a **typed module**: a `@dataclass` with `field(default_factory=list)`, an `@property`, EAFP
  parsing, and a `yield` **generator**.
- Wrote a **pytest** suite (including a `parametrize` sweep) and ran it **green** — the gate's behavior stage.
- Wrapped it in an **argparse CLI** — the entry-point idea that turns a function into a command.

**Back on the lesson page:** do the *Self-Check* (recall + teach-back) and tick the *Mastery checklist*.
When every box is honestly true, you've closed Stage 4's Python half and Module 14 (Networking) — which
fills Netscope's stubs into the packaged shell you now know how to build — becomes current.

> The one-sentence takeaway: **M13 turns M12's ability into engineering — typed, tested, packaged,
> gated — the difference between code that ran once and software that ships.**
