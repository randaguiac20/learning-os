# Learning OS — teaching platform site build dependencies.
# Install into the project venv ONLY (never system-wide):
#   /home/randagui/data/learning/.venv/bin/python -m pip install -r site/requirements.txt
# Versions pinned from the verified working build (2026-08-09).
mkdocs==1.6.1
mkdocs-material==9.7.7
mkdocs-material-extensions==1.3.1
pymdown-extensions==11.0.1
Pygments==2.20.0
